python-can>=4.3
# gs_usb backend (Candlelight / CANable) - needed only for --interface gs_usb
gs_usb>=0.3.0
pyusb>=1.2
# bundles libusb-1.0.dll so PyUSB has a backend on Windows (fixes NoBackendError)
libusb-package>=1.0.26
